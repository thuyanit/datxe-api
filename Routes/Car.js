// const express = require("express");
// const carController = require("../Controllers/Car");

// const {authenticate} = require('../Middlewares/Auth');

// const router = express.Router();


// router.post("/cars/register", authenticate, carController.createCar);

// router.get("/cars", authenticate, carController.getCars);

// router.get("/cars/:id", authenticate, carController.getCarByID);

// router.delete("/cars/:id", authenticate, carController.deleteCarByID);

// router.patch("/cars/:id", authenticate, carController.updateCarByID);


// module.exports = router;